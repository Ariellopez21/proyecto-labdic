% BASE
\documentclass{article}
\setlength{\headheight}{46.27916pt}
\usepackage{graphicx} % Required for inserting images
\usepackage[spanish]{babel} % espanol
\usepackage{graphicx}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{graphicx} 
\usepackage[spanish]{babel} 
%\usepackage[margin=1.4in]{geometry} % Margen de 1 pulgada en todo el documento

% Configurar encabezado con logo
%\usepackage{fancyhdr}
\fancypagestyle{mystyle}{
  \fancyhf{} % Limpiar encabezados y pies
  \fancyhead[L]{\includegraphics[height=1.5cm]{LogoDIC.jpg}} % Logo en esquina sup izquierda
  \renewcommand{\headrulewidth}{0pt} % Sin línea en encabezado
}

% Otros paquetes
\usepackage{setspace}
\usepackage{xcolor}
\usepackage{float}
\usepackage{caption}
\usepackage{subcaption}
    \captionsetup{justification=centering}
\usepackage{tabularx}   % tablas
\usepackage{array}      % tablas

\usepackage{pdfpages}   % Insertar pdf

% Para texttt
\usepackage{seqsplit}
\newcommand{\ttt}[1]{\texttt{\seqsplit{#1}}}	% Respetar saltos de linea.
% Ejemplo de uso: \ttt{loan\_request\_items.device.loan\_request\_items}



% Tikz
\usepackage{tikz}
\usetikzlibrary{positioning, shapes.multipart, arrows.meta, babel}

\definecolor{hdrbg}{HTML}{2563EB}
\definecolor{bodybg}{HTML}{EFF6FF}
\definecolor{bodyborder}{HTML}{2563EB}
\definecolor{pivothdr}{HTML}{CA8A04}
\definecolor{pivotbody}{HTML}{FEF9C3}
\definecolor{relfg}{HTML}{475569}

\tikzset{
	ent/.style={
		rectangle split, rectangle split parts=2,
		rectangle split part fill={hdrbg, bodybg},
		draw=bodyborder, thick, rounded corners=2pt,
		font=\scriptsize, text width=2.2cm, align=left,   % <-- cambiado
		inner sep=2pt,
		rectangle split part align={center, left},
	},
	piv/.style={
		rectangle split, rectangle split parts=2,
		rectangle split part fill={pivothdr, pivotbody},
		draw=pivothdr, thick, rounded corners=2pt,
		font=\scriptsize, text width=2.2cm, align=left,   % <-- cambiado
		inner sep=2pt,
		rectangle split part align={center, left},
	},
	rel/.style={draw=bodyborder, thick, -{Latex[length=2mm,width=1.5mm]}},
}

%uso matematico
%\usepackage{algorithm}
%\usepackage{algpseudocode}
%\renewcommand{\algorithmname}{Algoritmo}
%\renewcommand{\listalgorithmname}{Índice de algoritmos}

% Listings
\usepackage{listings}
\renewcommand{\lstlistingname}{Listado}
\usepackage{xcolor}
\usepackage{inconsolata} % fuente monoespaciada más bonita (opcional)

\renewcommand{\lstlistlistingname}{Índice de listados}
\usepackage{amsmath}

% ── Colores ──────────────────────────────────────────────────────────
\definecolor{codebg}   {HTML}{F4F4F8}
\definecolor{coderule} {HTML}{CFCDE8}
\definecolor{codekw}   {HTML}{5346B0} % palabras clave
\definecolor{codestr}  {HTML}{9E5A0F} % strings
\definecolor{codecom}  {HTML}{5C8A5C} % comentarios
\definecolor{codecmd}  {HTML}{B53A1E} % comandos shell
\definecolor{codenum}  {HTML}{1A7A5E} % números

% ── Estilo base ──────────────────────────────────────────────────────
\lstset{
	basicstyle      = \ttfamily\small,
	backgroundcolor = \color{codebg},
	rulecolor       = \color{coderule},
	frame           = single,
	framesep        = 4pt,
	xleftmargin     = 6pt,
	xrightmargin    = 6pt,
	breaklines      = true,
	breakatwhitespace = true,
	showstringspaces = false,
	tabsize         = 2,
	numbers         = left,
	numberstyle     = \tiny\color{gray},
	numbersep       = 8pt,
	captionpos      = b,
	keywordstyle    = \color{codekw}\bfseries,
	stringstyle     = \color{codestr},
	commentstyle    = \color{codecom}\itshape,
	literate        = {á}{{\'{a}}}1 {é}{{\'{e}}}1 {í}{{\'{i}}}1
	{ó}{{\'{o}}}1 {ú}{{\'{u}}}1 {ñ}{{\~{n}}}1,
}

% ── Estilo para comandos de terminal / bash ───────────────────────────
\lstdefinestyle{bash}{
	language        = bash,
	keywordstyle    = \color{codecmd}\bfseries,
	morekeywords    = {uv, bun, alembic, litestar, pgcli, createdb, source},
}

% ── Estilo para Python ───────────────────────────────────────────────
\lstdefinestyle{python}{
	language        = Python,
}

% ── Estilo para archivos de configuración / env ───────────────────────
\lstdefinestyle{env}{
	language        = {},
	morecomment     = [l]{\#},
	morestring      = [b]{"},
	commentstyle    = \color{codecom}\itshape,
	stringstyle     = \color{codestr},
}

% Hiper vínculos
\usepackage[hidelinks]{hyperref}

% Bibliografia
\usepackage{csquotes} % Para las comillas y citas.
\usepackage[backend=biber,style=ieee]{biblatex}
\addbibresource{bibliografia.bib}

